#spring:
#  application:
#    name: eureka-service
#  config:
#    import:
#      - optional:configserver:http://localhost:8012                  # <- pull the configuration from the configserver
#      - optional:file:/absolute/path/to/the/project/root/   # <- if there are any additional configuration files, use them

#management:
#  security:
#    enabled: false


#spring:
#  application:
#    name: greeting-service
#  config:
#    import: "optional:configserver:"
#  cloud:
#    config:
#      uri: http://localhost:8888
